[package]
name = "todo-rs"
version = "0.1.0"
edition = "2021"
description = "A blazingly fast CLI todo app with SQLite backend and due date tracking"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "todo-rs"
path = "src/main.rs"
